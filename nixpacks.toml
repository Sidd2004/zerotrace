# Railway / Nixpacks configuration
# Only npm install during build — no env vars needed at build time

[variables]
NODE_ENV = "production"

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install"]

[start]
cmd = "node server.js"